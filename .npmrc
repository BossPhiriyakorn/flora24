# ไม่ให้ npm install ล้มเมื่อ dependency (เช่น superstatic) ระบุ engine 20||22||24 ขณะใช้ Node 25
# ถ้าต้องการให้หาย warning: ใช้ Node 20 หรือ 22 (เช่น nvm use แล้วรัน npm install ใหม่)
engine-strict=false
